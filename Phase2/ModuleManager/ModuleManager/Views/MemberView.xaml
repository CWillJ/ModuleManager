<UserControl x:Class="ModuleManager.Views.MemberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:data="clr-namespace:ModuleManager.Classes"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="100">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type data:ModuleMember}">
            <StackPanel>
                <TextBox 
                    Text="{Binding Path=Name, Mode=OneWay}" Height="25" Width="100"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock TextAlignment="Center" Text="Description" />

        <Border BorderThickness="1" BorderBrush="Black" Margin="10,0" Height="275">
            <ScrollViewer Margin="10,5,0,0">
                <TextBlock x:Name="MemberText" Width="480" TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>
